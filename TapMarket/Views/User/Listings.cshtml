
@if (Model.Count == 0)
{
    <div class="jumbotron text-center">
        <h1 class="display-3">No listings</h1>
        <p class="lead">You don't have any <strong>listings</strong> yet! </p>
        <hr>
    </div>
}
else
{
    <h4 style="text-align:center"> My listings </h4>

    <div class="row">
        @foreach (var listing in Model)
        {
            <div class="btn col-6 col-sm-3">
                <div class="card" style="width: 16rem;">
                    <a href="/Listing/Details?listingId=@listing.Id" class="btn-light btn-light stretched-link">
                        <img class="card-img-top " src="@listing.ImageUrl" alt="Listing image">
                        <div class="card-body">
                            <h5 class="card-title">@listing.Title</h5>
                            <p class="card-text"> <b>$@listing.Price </b>- <i>@listing.Condition condition</i></p>
                        </div>
                    </a>
                </div>
            </div>
        }
    </div>
}